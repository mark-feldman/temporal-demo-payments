# Fail with "no matching toolchain" when no JDK 21 is available, rather than downloading
# one mid-build. `make` resolves one up front via scripts/java-home.sh.
org.gradle.java.installations.auto-download=false
org.gradle.jvmargs=-Xmx2g
org.gradle.caching=true
