# No network at the venue: fail fast with "no matching toolchain" rather than
# trying to download a JDK mid-demo.
org.gradle.java.installations.auto-download=false
org.gradle.jvmargs=-Xmx2g
org.gradle.caching=true
